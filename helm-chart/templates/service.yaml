apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-environments.fullname" . }}
  labels:
    {{- include "cloud-environments.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloud-environments.selectorLabels" . | nindent 4 }}
